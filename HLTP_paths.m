%% MATLAB paths for HLTP analysis

a = genpath(''); % local freesurfer directory
b = genpath('); % scripts directory
c = genpath(''); % toolbox directory (containing The Decoding Toolbox)
d = genpath(''); % spm directory

addpath(a,b,c,d);